directive @boundary on OBJECT | FIELD_DEFINITION

type Service {
  name: String!
  version: String!
  schema: String!
}

type Query {
  foos(ids: [ID!]!): [Foo!]! @boundary
  foo(id: ID!): Foo @boundary
  service: Service!
}

type Foo @boundary {
  id: ID!
  zoot: Boolean!
  bar: Boolean!
}
